一、protected 关键字的运用场景
1、protected 属性。
继承父类时，子类可以直接this.访问，而不会想private那样访问不到，而且也不用担心外部类读取得到属性。

2、重载，父类方法或者构造方法使用protected，子类在重载时可以放大，例如改为public，这样，同样在抽象类中可以保证抽象时属性或方法被外部过分使用，并且子类继承时需要/可以重载这个方法。

3、protected可以被this.和super.访问到，出去区分，父类的属性可以用super，子类用this